# 07 대시보드 및 변경관리 화면 패키지 승격

## 작업 목표

- [ ] `dashboard`, `change-management` 화면의 조합 UI를 `packages/components`로 승격한다.
- [ ] `web2`는 데이터 조회와 라우팅 제어만 남기고 화면 래퍼를 단순화한다.
- [ ] `apps/storybook`에 신규 화면 스토리를 추가한다.
- [ ] 단계 완료 후 build/lint/storybook 경로에서 회귀가 없는지 확인한다.

## 메모

- 이번 단계는 우선순위 2 기능군을 대상으로 한다.
- 설정 화면과 동일한 패턴으로 `packages/components` 화면 뷰 + `apps/web2` 연결 래퍼 구조를 만든다.
