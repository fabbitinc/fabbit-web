# 16 실화면 패리티 핵심 격차 2차 이관

## 작업 목표

- [ ] `dashboard`, `project detail`, `parts list`, `part detail`의 `web` 실화면 격차를 `packages/components` 기준으로 다시 줄인다.
- [ ] `issue detail`, `change detail`의 댓글/에디터 배치를 `web` 구현 기준으로 재정렬한다.
- [ ] `apps/web2`는 연결층만 남기고, 격차 수정은 우선 `packages/components`에 반영한다.
- [ ] 수정 후 동일 라우트 재캡처로 diff가 줄었는지 확인한다.

## 관찰 메모

- 2026-03-07 실캡처 기준 큰 diff 순서는 `parts-list`, `change-detail`, `part-detail`, `app-shell(dashboard)`, `issue-detail`, `project-detail`이다.
- `issue-create`, `change-create`, `project-list`는 상대적으로 차이가 작다.
- `parts-list`는 검색/필터 바와 상단 액션 구성이 아직 `web`와 다르다.
- `part-detail`은 상단 액션, drawing 카드 하단 액션, 탭 구성 세부가 아직 다르다.
- `dashboard`는 `web2` 쪽 welcome/user 카드 블록이 남아 있다.
- `project-detail`은 화면 조합 자체가 아직 `web`와 다른 요약형 레이아웃이다.
- `issue/change detail`은 댓글 목록과 댓글 작성기 배치가 `web`보다 단순화되어 있다.

## 검증 항목

- [ ] `test.lvh.me:5173` vs `test.lvh.me:5174` 재캡처
- [ ] `refactor/.captures/compare` diff 감소 확인
- [ ] 필요 시 `pnpm lint:web2`
- [ ] 필요 시 `pnpm build:web2`
