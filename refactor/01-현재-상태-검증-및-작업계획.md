# 01 현재 상태 검증 및 작업 계획

## 작업 목표

- [x] 현재 워크트리 기준 `web2`, `packages`, `storybook`의 빌드/타입 상태를 확인한다.
- [x] 이미 진행 중인 마이그레이션 변경을 기준으로 남은 작업 범위를 식별한다.
- [x] 후속 단계에서 다룰 작업을 `web2 컨벤션 정리`, `packages 덮어쓰기`, `검증` 단위로 확정한다.

## 메모

- 기존 워크트리에 대규모 마이그레이션 변경이 이미 존재한다.
- 기존 변경을 되돌리지 않고, 남은 누락과 깨진 지점을 수습하는 방향으로 진행한다.
- `pnpm build-storybook`는 성공했다.
- `pnpm build:web2`는 아래 오류로 실패했다.
  - `apps/web2/src/features/change-request/components/change-request-detail-screen.tsx`: `UserModel.fullName` 참조 오류
  - `apps/web2/src/features/issue/components/issue-detail-screen.tsx`: 미사용 import/type, `UserModel.fullName` 참조 오류
  - `packages/components/src/issue-sidebar.tsx`: 미사용 props 2건
  - `packages/ui/src/tiptap-editor.tsx`: 미사용 상수 1건
- 다음 단계는 `web2`와 `packages`의 타입 정리, 연결 경계 정리, 최종 빌드 검증으로 진행한다.
