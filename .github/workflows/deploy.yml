name: Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    name: Trigger Cloudflare Pages Build
    runs-on: ubuntu

    steps:
      - name: Trigger Cloudflare build webhook
        run: |
          curl -sf -X POST "${{ secrets.CLOUDFLARE_BUILD_WEBHOOK_URL }}"
